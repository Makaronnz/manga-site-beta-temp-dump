// src/components/AutoSaveProgress.tsx
/**
 * INFO:
 * No-op placeholder. We DON’T auto-save on reader open anymore.
 * Keep this component mounted safely (does nothing).
 */
"use client";
export default function AutoSaveProgress(_: { seriesId: string | number; chapterId: string | number; chapter?: string | null }) {
  return null;
}
